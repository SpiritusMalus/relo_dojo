# Expo SDK 54 (RN 0.81 + react 19) has peer-dependency ranges that npm's strict resolver rejects
# for some dev deps (e.g. jest-expo). Default to legacy peer-dep resolution so a plain `npm install`
# works for everyone and Expo's dependency check passes.
legacy-peer-deps=true
