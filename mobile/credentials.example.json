{
  "_comment": "TEMPLATE ONLY — copy to credentials.json (gitignored) and fill in your local release keystore. Used by `eas build --local --profile prod-local`. Never commit credentials.json or the .keystore file.",
  "android": {
    "keystore": {
      "keystorePath": "credentials/relodojo-release.keystore",
      "keystorePassword": "REPLACE_WITH_KEYSTORE_PASSWORD",
      "keyAlias": "relodojo",
      "keyPassword": "REPLACE_WITH_KEY_PASSWORD"
    }
  }
}
