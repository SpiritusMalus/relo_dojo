# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies (bare form too: in git worktrees node_modules is a symlink, which `node_modules/`
# — a directory-only pattern — does not match)
node_modules/
node_modules

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Local release signing (sideload prod-local build) — NEVER commit the keystore or its passwords.
# Commit only credentials.example.json + the eas.json wiring. See README "Local production build".
*.keystore
credentials.json
credentials/

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android
