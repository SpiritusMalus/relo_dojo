# Backend environment template. Copy to .env and fill real values (never commit .env).
# Phase 0 needs none of these yet; placeholders for upcoming phases.

# Phase 1+ — Ollama
# OLLAMA_URL=http://localhost:11434

# Phase 4+ — auth / db
# JWT_SECRET=
# DATABASE_URL=
