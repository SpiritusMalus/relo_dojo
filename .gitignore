# === Claude / agent files (never commit) ===
CLAUDE.md
.claude/

# === Python (backend) ===
backend/.venv/
.venv/
.venv*/
backend/.venv*/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# === Node / Expo (mobile) ===
node_modules/
mobile/node_modules/
.expo/
mobile/.expo/
dist/
web-build/
*.tsbuildinfo

# === Secrets / env ===
.env
*.env
# Also ignore env-as-prefix files (deploy convention: .env.local, .env.prod, .env.production) —
# `*.env` only catches name.env, not these. The shipped *.env*.example templates stay tracked.
.env.*
!*.env.example
!.env.*.example

# === OS / editor ===
.DS_Store
*.log
